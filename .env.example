# Choose one database mode:
# SQLite is easiest for local development.
SQLITE_FILE=./dev.db

# Uncomment and set a Postgres connection string for production.
# DATABASE_URL=postgres://user:pass@localhost:5432/mydb

JWT_SECRET=replace-with-a-secret

# Optional resume contact details.
# These are read by the server and only returned to authenticated admins.
RESUME_NAME=
RESUME_TITLE=
RESUME_EMAIL=
RESUME_PHONE=
RESUME_LOCATION=
RESUME_WEBSITE=
RESUME_LINKEDIN=
RESUME_GITHUB=

# OAuth providers
GITHUB_CLIENT_ID=
GITHUB_CLIENT_SECRET=
OAUTH_REDIRECT_URI=http://localhost:3000/api/auth/oauth/github/callback
